---
kind: Service
apiVersion: v1
metadata:
  name: votes
  namespace: my-project
  labels:
    app.kubernetes.io/name: votes
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 50052
    name: grpc
  selector:
    app.kubernetes.io/name: votes
